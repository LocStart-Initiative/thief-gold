# read file tree map file
# read folder with intermediary files
# read and then copy folder with source files

# use the same regexes to find and replace from xlsx into source file
# all things being the same the number of matches will be the same
# so all that needs doing is joining split multiline entries
# then replace everything in order